# Validação do módulo opcional

A pasta `cloneYou/opcionais/fila-inteligente` foi aberta no navegador. Os caminhos relativos para `css/style.css` e `js/script.js` carregaram corretamente.

Ao adicionar o primeiro vídeo, a interface atualizou a quantidade na fila para `1`, calculou o tempo total como `12:34`, preencheu o player demonstrativo, alterou o botão para `Na fila`, renderizou o item no painel `Próximos` e exibiu uma mensagem de confirmação.

O módulo `interacoes-video` também foi aberto e testado. A ação `Curtir` atualizou o contador para `1` e exibiu confirmação. O formulário criou um comentário com o texto informado, atualizou o contador de comentários e renderizou o botão de remoção.
